{
  "name": "sandbox-sim",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "cd .. && git add . && git commit -m \"Auto-deploy: $(date)\" && git push origin devin/1747592774-initial-project-setup"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
